version: "3.8"
services:
  web:
    build: .
    container_name: roma-shopping
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=development
      - FLASK_PORT=5000
    restart: unless-stopped
